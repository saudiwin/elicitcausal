% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elicit_cpt.R
\name{elicit_node_cpt}
\alias{elicit_node_cpt}
\title{Elicit a conditional probability table for a single node}
\usage{
elicit_node_cpt(
  node,
  parent_names,
  mode = "probability",
  labels = NULL,
  .responses = NULL
)
}
\arguments{
\item{node}{Character. Name of the node}

\item{parent_names}{Character vector. Names of parent nodes (may be
length-0 for root nodes)}

\item{mode}{Character. "probability" or "likert"}

\item{.responses}{Optional list of pre-specified responses, one per parent
combination row (in the same order as \code{expand.grid} with 0/1
values). Root nodes expect a single-element list.}
}
\value{
A data frame of class \code{c("cpt", "data.frame")} with one
  column per parent (integer 0/1), a \code{prob} column
  (\eqn{P(\text{node}=1|\ldots)}), and attributes \code{"node"} and
  \code{"parents"}.
}
\description{
Iterates over all combinations of parent values (in binary counting order:
00, 01, 10, 11, …) and prompts the user for \eqn{P(\text{node}=1 |
\text{parents})}. For root nodes (no parents) a single prior probability
is collected.
}
\keyword{internal}

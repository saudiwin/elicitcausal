% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_utils.R
\name{get_conditional}
\alias{get_conditional}
\title{Compute a conditional probability P(query | evidence)}
\usage{
get_conditional(result, query, evidence = integer(0L))
}
\arguments{
\item{result}{An \code{elicit_dag_result} object}

\item{query}{Named integer vector. Query variables and their values (0/1)}

\item{evidence}{Named integer vector. Evidence variables and their values
(0/1). Default: empty (no conditioning).}
}
\value{
Numeric probability in [0, 1], or \code{NA} if the evidence has
  zero probability
}
\description{
Filters the joint distribution to rows consistent with the evidence and
sums over rows also consistent with the query variables.
}
\examples{
\dontrun{
# P(Y = 1 | X = 1)
get_conditional(result, query = c(Y = 1L), evidence = c(X = 1L))
}
}
